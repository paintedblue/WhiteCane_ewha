# 서버 포트 (기본: 8000)
PORT=8000

# 지도 라우팅 서버 URL (경로 계산 API 연동 시 설정)
ROUTING_SERVER_URL=http://localhost:8002

# 데이터베이스 URL (장소 데이터 연동 시 설정)
DATABASE_URL=postgresql://user:password@localhost:5432/whitecane_db
